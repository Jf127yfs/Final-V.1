<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>PARTY SYSTEM // 5317 CHARLOTTE</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root {
      --bg: #000;
      --fg: #0f0;
      --accent: #ff0;
      --warn: #f00;
      --panel: #0a0a0a;
      --border: #0f0;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background: var(--bg);
      color: var(--fg);
      font: 14px 'Courier New', monospace;
      overflow: hidden;
      height: 100vh;
      display: flex;
      flex-direction: column;
      position: relative;
    }

    /* Scanline effect */
    body::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: repeating-linear-gradient(
        0deg,
        rgba(0, 255, 0, 0.03) 0px,
        rgba(0, 0, 0, 0.05) 1px,
        rgba(0, 255, 0, 0.03) 2px
      );
      pointer-events: none;
      z-index: 9999;
      animation: scanline 8s linear infinite;
    }

    @keyframes scanline {
      0% { transform: translateY(0); }
      100% { transform: translateY(10px); }
    }

    /* CRT flicker */
    @keyframes flicker {
      0% { opacity: 0.97; }
      5% { opacity: 1; }
      10% { opacity: 0.97; }
      100% { opacity: 0.97; }
    }

    body {
      animation: flicker 0.15s infinite;
    }

    .header {
      text-align: center;
      padding: 20px;
      border-bottom: 2px solid var(--border);
      background: var(--panel);
      box-shadow: 0 0 20px rgba(0, 255, 0, 0.3);
    }

    .header h1 {
      font-size: 28px;
      letter-spacing: 8px;
      color: var(--fg);
      text-shadow: 0 0 10px var(--fg), 0 0 20px var(--fg);
      margin-bottom: 8px;
    }

    .header .subtitle {
      font-size: 11px;
      color: var(--accent);
      letter-spacing: 4px;
      text-shadow: 0 0 5px var(--accent);
    }

    .header .system-status {
      margin-top: 12px;
      font-size: 10px;
      color: var(--fg);
      opacity: 0.7;
    }

    .header .system-status span {
      color: var(--accent);
      font-weight: bold;
    }

    .intro-ticker {
      background: var(--panel);
      border-bottom: 2px solid var(--border);
      padding: 12px 0;
      overflow: hidden;
      position: relative;
      box-shadow: inset 0 0 20px rgba(0, 255, 0, 0.1);
    }

    .intro-ticker-content {
      display: inline-block;
      white-space: nowrap;
      padding-left: 100%;
      animation: scroll-left 35s linear infinite;
      font-size: 12px;
      color: var(--fg);
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    @keyframes scroll-left {
      0% { transform: translateX(0); }
      100% { transform: translateX(-100%); }
    }

    .intro-ticker:hover .intro-ticker-content {
      animation-play-state: paused;
    }

    .main-container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px;
      background: var(--bg);
      position: relative;
      overflow: auto;
    }

    .menu-container {
      max-width: 800px;
      width: 100%;
      background: var(--panel);
      border: 2px solid var(--border);
      padding: 30px;
      box-shadow: 0 0 30px rgba(0, 255, 0, 0.4);
    }

    .menu-header {
      text-align: center;
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid var(--border);
    }

    .menu-header h2 {
      font-size: 20px;
      letter-spacing: 4px;
      color: var(--fg);
      text-shadow: 0 0 10px var(--fg);
      margin-bottom: 10px;
    }

    .menu-header p {
      font-size: 11px;
      color: var(--accent);
      letter-spacing: 2px;
    }

    .menu-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 20px;
      margin-bottom: 30px;
    }

    .menu-item {
      background: var(--bg);
      border: 2px solid var(--border);
      padding: 20px;
      cursor: pointer;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .menu-item::before {
      content: ">";
      position: absolute;
      left: -20px;
      top: 50%;
      transform: translateY(-50%);
      font-size: 20px;
      color: var(--accent);
      transition: left 0.3s ease;
    }

    .menu-item:hover {
      background: rgba(0, 255, 0, 0.1);
      border-color: var(--accent);
      box-shadow: 0 0 20px rgba(0, 255, 0, 0.3);
      transform: translateX(5px);
    }

    .menu-item:hover::before {
      left: 10px;
    }

    .menu-item:active {
      transform: translateX(8px);
      box-shadow: 0 0 30px rgba(0, 255, 0, 0.5);
    }

    .menu-item h3 {
      font-size: 16px;
      letter-spacing: 3px;
      color: var(--fg);
      margin-bottom: 10px;
      text-shadow: 0 0 5px var(--fg);
    }

    .menu-item p {
      font-size: 11px;
      color: var(--accent);
      line-height: 1.6;
      letter-spacing: 1px;
    }

    .menu-item .status {
      margin-top: 10px;
      font-size: 9px;
      color: var(--fg);
      opacity: 0.6;
      text-transform: uppercase;
    }

    .menu-item .status.active {
      color: var(--accent);
      opacity: 1;
    }

    .footer {
      text-align: center;
      padding: 15px;
      border-top: 2px solid var(--border);
      background: var(--panel);
      font-size: 10px;
      color: var(--fg);
      opacity: 0.7;
    }

    .footer .blink {
      animation: blink 1.5s infinite;
      color: var(--accent);
    }

    @keyframes blink {
      0%, 49% { opacity: 1; }
      50%, 100% { opacity: 0; }
    }

    /* Loading animation */
    .loading {
      display: none;
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 16px;
      color: var(--fg);
      text-shadow: 0 0 10px var(--fg);
      z-index: 10000;
      background: var(--bg);
      padding: 20px 40px;
      border: 2px solid var(--border);
      box-shadow: 0 0 30px rgba(0, 255, 0, 0.5);
    }

    .loading.active {
      display: block;
    }

    .loading::after {
      content: "...";
      animation: dots 1.5s infinite;
    }

    @keyframes dots {
      0%, 20% { content: "."; }
      40% { content: ".."; }
      60%, 100% { content: "..."; }
    }
  </style>
</head>
<body>
  <div class="header">
    <h1>PARTY SYSTEM v2.0</h1>
    <div class="subtitle">5317 CHARLOTTE // KANSAS CITY, MO</div>
    <div class="system-status">
      SYSTEM STATUS: <span id="systemStatus">ONLINE</span> //
      TIME: <span id="currentTime">--:--:--</span>
    </div>
  </div>

  <div class="intro-ticker">
    <div class="intro-ticker-content" id="introTicker">
      LOADING SYSTEM MESSAGE...
    </div>
  </div>

  <div class="main-container">
    <div class="menu-container">
      <div class="menu-header">
        <h2>MAIN MENU</h2>
        <p>SELECT A MODULE TO ACCESS</p>
      </div>

      <div class="menu-grid">
        <div class="menu-item" onclick="navigate('intro')">
          <h3>INTRODUCTION</h3>
          <p>System overview and welcome message. Learn about the party system and features available.</p>
          <div class="status">STATUS: READY</div>
        </div>

        <div class="menu-item" onclick="navigate('map')">
          <h3>MAP VIEW</h3>
          <p>Geographic visualization of guest locations. Interactive mapping interface.</p>
          <div class="status active">STATUS: ACTIVE</div>
        </div>

        <div class="menu-item" onclick="navigate('msa')">
          <h3>MSA ANALYSIS</h3>
          <p>Metropolitan Statistical Area analysis and demographic insights.</p>
          <div class="status">STATUS: BETA</div>
        </div>

        <div class="menu-item" onclick="navigate('network')">
          <h3>NETWORK GRAPH</h3>
          <p>Social network visualization showing guest connections and relationships.</p>
          <div class="status">STATUS: BETA</div>
        </div>
      </div>

      <div style="text-align: center; margin-top: 20px; padding-top: 20px; border-top: 1px solid var(--border);">
        <p style="font-size: 10px; color: var(--accent); letter-spacing: 2px;">
          USE NAVIGATION ABOVE TO ACCESS SYSTEM MODULES
        </p>
      </div>
    </div>
  </div>

  <div class="footer">
    <span class="blink">█</span> SYSTEM READY // AWAITING INPUT
  </div>

  <div class="loading" id="loading">LOADING MODULE</div>

  <script>
    // Update current time
    function updateTime() {
      const now = new Date();
      const timeString = now.toLocaleTimeString('en-US', { hour12: false });
      document.getElementById('currentTime').textContent = timeString;
    }

    setInterval(updateTime, 1000);
    updateTime();

    // Load intro text
    google.script.run
      .withSuccessHandler(function(text) {
        document.getElementById('introTicker').textContent = text || 'WELCOME TO THE PARTY SYSTEM // 5317 CHARLOTTE // KANSAS CITY, MO // ENJOY YOUR EXPERIENCE';
      })
      .withFailureHandler(function(error) {
        document.getElementById('introTicker').textContent = 'WELCOME TO THE PARTY SYSTEM // 5317 CHARLOTTE // KANSAS CITY, MO';
      })
      .getIntroText();

    // Navigation function
    function navigate(page) {
      const loading = document.getElementById('loading');
      loading.classList.add('active');

      // Simulate terminal loading effect
      setTimeout(function() {
        window.location.href = '?page=' + page;
      }, 500);
    }

    // System status indicator
    function updateSystemStatus() {
      const statuses = ['ONLINE', 'ACTIVE', 'OPERATIONAL'];
      const randomStatus = statuses[Math.floor(Math.random() * statuses.length)];
      document.getElementById('systemStatus').textContent = randomStatus;
    }

    setInterval(updateSystemStatus, 5000);

    // Console easter egg
    console.log('%c5317 CHARLOTTE PARTY SYSTEM', 'color: #0f0; font-size: 20px; font-family: monospace; text-shadow: 0 0 10px #0f0;');
    console.log('%cWelcome to the matrix...', 'color: #ff0; font-family: monospace;');
  </script>
</body>
</html>
